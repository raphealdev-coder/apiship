{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_ConnectionStrings": "Set via the DATABASE_URL env var (Render) or ConnectionStrings:Default (local). Postgres/Npgsql format.",
  "Admin": {
    "Username": "admin",
    "_comment": "Set the password via user-secrets or the ADMIN_PASSWORD env var. Never commit a real password here."
  }
}
